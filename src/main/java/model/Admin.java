package model;

public class Admin extends User {
    public Admin(int UserId, String Name, String email, String phoneNumber, String username, String password) {
        super(UserId, Name, email, phoneNumber, username, password);
    }
}
